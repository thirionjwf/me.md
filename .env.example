# MiniMax Token Plan API key (OpenAI-compatible endpoint at api.minimax.io/v1).
# Can also be set in the app's Settings page. The env-var name is kept for backward compat.
VITE_ANTHROPIC_API_KEY=your-minimax-api-key-here
